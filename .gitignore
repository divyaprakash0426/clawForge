# ── Internal / design artefacts ──────────────────────────────────────────────
clawforge_blueprint.txt
docs/dev-article-draft.md

# ── Local emulation / simulation infrastructure ───────────────────────────────
# All VM harness files, rootfs snapshots, QEMU binaries, and run artefacts are
# kept outside git; they live under .local/ and can be regenerated at any time.
.local/

# ── Automated scan / cost report outputs ─────────────────────────────────────
# These are generated by local tooling and should not be committed.
secret-findings.json
cost-report.json

# ── Runtime / scratch artefacts ───────────────────────────────────────────────
*.pyc
__pycache__/
*.egg-info/
.env
.env.*
!.env.example

# ── OS / editor noise ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
